# Dependencies are pinned to exact versions, never a range. A caret range
# lets a patch release change what the linter reports, which in a repository
# that forbids lint configuration means the build can turn red without any
# content changing. npm test fails if a range appears.
save-exact=true
