# The Node version itself lives in .node-version, not here, because that is
# the format actions/setup-node can read: it matches a bare version on a
# line and does not parse TOML, so a version written here would be invisible
# to continuous integration. This file exists only to tell mise to honour
# that file, which recent versions do not do by default.
#
# Node is the only tool needed. The two validation scripts run on it, and
# markdownlint-cli2 comes from npm.

[settings]
idiomatic_version_file_enable_tools = ["node"]
